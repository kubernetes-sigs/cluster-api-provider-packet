package util

const (
	// CAPPNamespace must match what is in kustomization.yaml
	CAPPNamespace = "cluster-api-provider-packet-system"
	// CAPPPrefix must match what is in kustomization.yaml
	CAPPPrefix = "cluster-api-provider-packet-"
)
